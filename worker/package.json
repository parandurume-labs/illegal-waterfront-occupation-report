{
  "name": "illegal-occupation-report-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute illegal-occupation-report-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
